# About

This program calculate one function called here as Utility. For this calculations the file with graphs encoded in ASCII characters is taken (graph6 format). Such files could be found here: http://users.cecs.anu.edu.au/~bdm/data/graphs.html
Then the program takes each of them one by one, calculates Utility, generate TeX-code and writes it in TeX-file. After compilation of TeX-file a visual result could be produced.

## How to run it?

For running the sage should be installed.
In terminal should be writen:

```bash
sage path-to-utility_calc.sage path-to-graph6-file path-to-tex-file
```